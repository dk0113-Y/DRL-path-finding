# Final Method Records

This directory is reserved for lightweight curated records from the active A_new
final method and Anew_R1-Anew_R5 reward ablations.

Do not commit smoke outputs, checkpoint bodies, raw `outputs/` run directories, or
`.pt`/`.pth`/`.ckpt` files here. Legacy experiment records were archived at
`legacy/pre-a-new-cleanup` and `legacy-pre-a-new-cleanup-20260525`.
